{% extends 'base.html.twig' %}

{% block title %}Liste des Entreprises{% endblock %}

{% block contenu %}
<h1 class="h3 mb-3 offset-1">Liste des Entreprises - <a href='{{path('entreprise_ajout')}}'>Ajouter une Entreprise</a></h1>
<div class="table-responsive offset-1">
    <table class="table table-hover table-striped">
        <thead>
          <tr>
            <th scope="col">Id</th>
            <th scope="col">Nom</th>
            <th scope="col">Adresse</th>
            <th></th>
          </tr>
        </thead>
    {% if not liste is empty %}
        {{ form_start(form)}}
        <tbody>
            {% for u in liste %}
                <tr>
                    <td scope="row"><a href="{{ path('entreprise_modifier',{'id':u.id})}}">{{u.id}}</a></th>
                    <td>{{u.nom}}</td>
                    <td>{{u.adresse}}</td>
                    <td><input type="checkbox" name="cocher[]"  value="{{u.id}}"/></td>
                </tr>
            {% endfor %}
    {% else %}
        <tr><td>Aucune entreprise à afficher</td><td></td><td></td><td></td></tr>
    {% endif %}
        </tbody>
    </table>
    {{ form_widget(form.save) }}
    {{ form_end(form)}}
</div>
{% endblock %}
