{% extends 'base.html.twig' %}

{% block title %}Modifier du vocabulaire{% endblock %}

{% block contenu %}
    
    <h1 class="h3 mb-3 font-weight-normal">Modification du vocabulaire</h1>
    <div align="center">
    {{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="form-row">
        {{ form_label(form.libelle, 'Libellé:', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-10">
          {{ form_errors(form.libelle) }}
          {{ form_widget(form.libelle) }}
          {{ form_help(form.libelle) }}
        </div>  
    </div>
        <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            {{ form_widget(form.save) }}
        </div>
    </div>
        <input type="hidden" id="form__token" name="form[_token]" value="Rt5KAlurW04xwwW9tuwHpfvxObmNrfhR0cI7zkXV38I" />    
        {{ form_end(form) }}
    </div>
    
{% endblock %}